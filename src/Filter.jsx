import {Container, Typography, CardMedia, Card, CardActions, CardContent, Button, Box, Paper, IconButton, SvgIcon, FontAwesomeSvgIcon, Icon } from '@mui/material';


function Filter(props) {

  const {projects} = props
  console.log(projects)
  return (
    <Container>
    </Container>
  )
}

export default Filter;